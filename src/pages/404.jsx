import { useNavigate } from 'react-router-dom';

function Page404() {
  const navigate = useNavigate();

  const goHome = () => {
    navigate('/')
  }
  return (
    </div>
  );
}

export default Page404;